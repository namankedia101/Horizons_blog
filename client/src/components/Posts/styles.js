import { makeStyles } from "@material-ui/core/styles";

export default makeStyles ((theme)=>({
    container:{
        marginTop:"6rem",
        justifyContent:"center",
        width: "100%"
    },
    post:{
        display: "flex",
        marginBottom:"3rem"
    },
    progress:{
        marginTop:"2rem"
    }
}))